# Todo Tutor 68 前端修复说明

## 这次主要修了什么

1. 把认证接口地址改成了可配置的 `VITE_API_URL`，默认回落到后端文档里的 `http://127.0.0.1:8088`
2. 收口了认证请求的错误处理，避免接口地址失效时前端只报模糊错误
3. 统一了登录返回数据的用户映射，兼容后端返回的 `userId` 和 `userDO`
4. 移除了登录页里重复且容易误导的“账号密码登录”表单，只保留后端真实支持的三种方式
5. 注册成功后改成明确提示“返回登录页登录”，并清空敏感输入
6. 在个人设置页补了退出登录按钮，方便联调完整登录态
7. 清理了 Lovable 初始化留下的说明，改成当前项目的本地运行说明

## 为什么之前容易出问题

- 前端把 API 地址写死到了一个当前环境无法解析的域名，导致注册和登录请求发不出去
- 后端认证返回的是 `userId`、`token`、`userDO` 结构，前端直接当成页面里的 `user.id` 在用，登录后用户信息不完整
- 登录页同时放了两套几乎一样的用户名密码表单，容易让人误以为后端还支持另一种账号密码接口
- 仓库里还残留了 Supabase 目录，虽然主流程没在用，但会让后续维护者误判数据流

## 联调建议

1. 先启动后端，并确认接口基地址
2. 前端 `.env` 里的 `VITE_API_URL` 对齐后端地址
3. 在注册页依次测试：发送注册验证码 -> 注册
4. 在登录页依次测试：用户名密码登录、邮箱验证码登录、匿名登录
5. 在个人设置页测试退出登录，确认能回到 `/auth`

## 这次没法直接替你验证的部分

- 当前机器上 `127.0.0.1:8088` 没有启动后端，所以我只能完成前端修复和构建验证，不能替你真实收验证码做端到端注册/登录
